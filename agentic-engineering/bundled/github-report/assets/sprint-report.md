# <Scope> — Work Report

| | |
| --- | --- |
| **Scope** | <Sprint 24 / v1.4.0 / Billing feature> |
| **Range** | <YYYY-MM-DD> → <YYYY-MM-DD> |
| **Repo / branch** | <owner/name> @ `main` |
| **Generated** | <YYYY-MM-DD> |

## Summary

<3–5 sentences. What shipped, what slipped, and the one thing the reader needs to know. Written so someone who reads nothing else is not misinformed.>

## At a glance

| | |
| --- | --- |
| PRs merged | <n> |
| Issues closed | <n> |
| Contributors | <n> |

## Breaking changes

<Delete this section if nothing broke.>

- **<What broke>** — <what callers must change>. ([PR #<n>](<url>))

## Delivered

### <Group 1 — feature / area / module>

- **<Outcome>** — <one sentence: what is now true, in the reader's vocabulary>. ([#<n>](<url>), [PR #<n>](<url>))
- **<Outcome>** — <…>. ([PR #<n>](<url>))

### <Group 2>

- **<Outcome>** — <…>. ([#<n>](<url>))

### Other

<Items that did not fit a group. Keep it short; a long Other means the grouping axis is wrong.>

## Fixes

- **<What was broken, and for whom>** — <what now happens instead>. ([#<n>](<url>))

## Internal

<Refactors, infra, tooling, docs. Delete for stakeholder and release-notes audiences.>

- <Change> ([PR #<n>](<url>))

## Not landed

| Item | Status | Reason |
| --- | --- | --- |
| [#<n>](<url>) <title> | Carried over | <why> |
| [#<n>](<url>) <title> | Reverted | <why> ([PR #<n>](<url>)) |
| [#<n>](<url>) <title> | Closed, not planned | <why> |

## Caveats

- <Data gaps: e.g. "PR bodies unavailable — `gh` not authenticated; bullets derived from commit subjects.">
- <Inferred groupings: e.g. "12 commits had no scope; grouped by file path.">
- <Excluded: e.g. "9 lockfile/formatting commits dropped.">
